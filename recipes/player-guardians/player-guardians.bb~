DESCRIPTION = "Player"
LICENSE = "GPL"
DEPENDS = "libtool"

SRC_URI = "file://player-guardians-2.1.1.tar.bz2"
S = "${WORKDIR}/player-guardians-${PV}"

EXTRA_OECONF = "--disable-alldrivers --enable-chemical"
PARALLEL_MAKE = ""
LEAD_SONAME = "libplayer*"
FILES_${PN} = "${bindir} ${libdir}/*.so.${PV}"

inherit autotools

